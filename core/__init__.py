"""Game-agnostic models and repositories.

Anything that's owned by no single mini-game (cross-game configuration,
analytics, command logging) lives here so that adding a new game doesn't
require editing files inside another game's module.
"""
